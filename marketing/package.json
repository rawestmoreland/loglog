{
  "name": "marketing",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build ./index.ts --outdir ./dist",
    "watch": "bun build --watch ./index.ts --outdir ./dist",
    "dev": "bun build --watch ./index.ts --outdir ./dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.0.13",
    "tailwindcss": "^4.0.13"
  }
}
